@extends('layouts.app')

@section('title', 'Export PDF')

@section('content')
 <title>Quiz Results Report</title>
    <style>
        body {
            font-family: DejaVu Sans, sans-serif;
            font-size: 12px;
            color: #333;
        }
        .header {
            text-align: center;
            border-bottom: 2px solid #4a6cf7;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        .header img {
            width: 80px;
            height: 80px;
        }
        h1, h2, h3, h4 {
            margin: 0;
        }
        .school-name {
            font-size: 20px;
            font-weight: bold;
            color: #4a6cf7;
        }
        .subtitle {
            font-size: 14px;
            color: #555;
        }
        .meta {
            text-align: right;
            font-size: 12px;
            margin-bottom: 10px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }
        th, td {
            border: 1px solid #333;
            padding: 8px;
            text-align: left;
        }
        th {
            background: #e4e9f7;
        }
        .footer {
            margin-top: 20px;
            text-align: center;
            font-size: 11px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="header">
        @if (file_exists(public_path('images/psau logo.png')))
            <img src="{{ public_path('images/psau logo.png') }}" alt="Logo">
        @endif
        <div class="school-name">PAMPANGA STATE AGRICULTURAL UNIVERSITY</div>
        <div class="subtitle">Department of Information Technology</div>
    </div>

    <div class="meta">
        <strong>Date:</strong> {{ now()->format('Y-m-d') }}<br>
        <strong>Generated by:</strong> {{ auth()->user()->name }}
    </div>

    <h3 style="color:#4a6cf7;">📊 Quiz Results Report</h3>

    <table>
        <thead>
            <tr>
                <th>Student Name</th>
                <th>Quiz Title</th>
                <th>Score</th>
                <th>Date Taken</th>
            </tr>
        </thead>
        <tbody>
            @foreach ($results as $result)
                <tr>
                    <td>{{ $result->user->name }}</td>
                    <td>{{ $result->quiz->title }}</td>
                    <td>{{ $result->score }}</td>
                    <td>{{ $result->created_at->format('Y-m-d H:i') }}</td>
                </tr>
            @endforeach
        </tbody>
    </table>

    <div class="footer">
        Generated automatically by PSAU Quiz System
    </div>
</body>
</html>

@endsection